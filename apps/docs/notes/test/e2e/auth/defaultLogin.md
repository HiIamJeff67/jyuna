### Procedures :
* User can login with no constraints, but if the form is invalid, the response will always be negative

* User can login with a valid, registered user name and password
	* Use user name as the account field

* User can login with a valid, registered display name and password
	* Use display name as the account field

* User can login with a valid, registered email and password
	* Use email as the account field